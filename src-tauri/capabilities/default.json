{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main desktop capability for the festival scheduler window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
